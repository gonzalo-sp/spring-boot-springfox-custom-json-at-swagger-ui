spring.application.name=cars-example
# this profile will be used to fetch properties from configuration server
spring.profiles.active=local

# == Configuration ==
# if you want to use the config server, you need to activate these variables
# N.B. this is the default:
#spring.cloud.config.uri=http://localhost:8888
# disables server security (by default)
#management.security.enabled=false
# This is needed if the config server authorization is enabled
#spring.cloud.config.username=root
#spring.cloud.config.password=s3cr3t

# Otherwise you will need a file:
# src/main/resources/application-${spring.profiles.active}.properties
# ====
